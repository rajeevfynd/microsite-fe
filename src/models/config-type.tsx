// Editable config type new data configs can be added with generic type to 
// handle the request configuration
export type configType = {
    headerAuthorization : string,
    contentType : string
}
